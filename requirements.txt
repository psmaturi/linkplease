# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Data validation
pydantic==2.7.4
pydantic-settings==2.3.4

# Database
sqlalchemy==2.0.31
asyncpg==0.29.0          # async PostgreSQL driver
alembic==1.13.2          # schema migrations

# Redis (async)
redis==5.0.7

# HTTP client (async, for calling PseudoGram)
httpx==0.27.0

# Utilities
python-dotenv==1.0.1

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0
httpx==0.27.0            # also used as test client
anyio==4.4.0

# Linting / typing (dev only, not needed at runtime)
# mypy ruff — install separately if desired
